# Gradle / build outputs
.gradle/
build/
captures/
.externalNativeBuild/
.cxx/
*.aab
*.apk
output-metadata.json

# Local configuration (sdk path, etc.)
local.properties

# IntelliJ / Android Studio
*.iml
.idea/
misc.xml
deploymentTargetDropDown.xml
render.experimental.xml

# Keystore files (release signing must never be committed)
*.jks
*.keystore
keystore.properties

# Google Services
google-services.json

# JVM / Kotlin
*.class
*.log
.kotlin/
*.hprof
hs_err_pid*
replay_pid*

# OS junk
.DS_Store
Thumbs.db

# Package archives
*.jar
*.war
*.nar
*.ear
*.zip
*.tar.gz
*.rar
# Exception: the Gradle wrapper jar MUST be checked in so the project is
# cloneable and buildable on CI without a pre-installed Gradle.
!gradle/wrapper/gradle-wrapper.jar

# Misc
*.ctxt
.mtj.tmp/

# Local working notes — never commit
.notes/
